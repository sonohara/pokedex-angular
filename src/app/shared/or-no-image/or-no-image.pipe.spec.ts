import { OrNoImagePipe } from './or-no-image.pipe';

describe('OrNoImagePipe', () => {
  it('create an instance', () => {
    const pipe = new OrNoImagePipe();
    expect(pipe).toBeTruthy();
  });
});
